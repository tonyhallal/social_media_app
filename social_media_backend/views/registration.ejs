<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<section class="h-screen flex items-center justify-center">
    <div class="rounded-lg bg-blue-500 px-8 py-10 flex flex-col gap-y-8 w-1/3">
        <form action="/api/v1/auth" method="post" class="flex flex-col gap-y-8">
            <h1
                    class="w-full text-center text-4xl text-white font-bold">Sign In</h1>
            <div class="flex flex-col gap-y-1"><input
                        class="border-b-2 bg-transparent border-b-white p-1 text-white placeholder:text-white  focus:outline-none"
                        autofocus type="text" name="user_username" placeholder="enter username" id="username-input"
                        required>
            </div>
            <input class="border-b-2 bg-transparent border-b-white p-1 text-white placeholder:text-white  focus:outline-none"
                   type="password" name="user_password" autocomplete placeholder="enter password" id="password-input"
                   required>

            <button type="submit" id="submit-btn"
                    class="mx-auto text-blue-500 bg-white rounded py-3 px-7 font-bold hover:bg-blue-900 hover:text-white duration-300">
                Sign In
            </button>
        </form>
        <span id="other-option" class="text-white font-bold mx-auto">Don't have an account? <button id="handle-mode-btn"
                                                                                                    class="underline hover:opacity-70 duration-300">Sign Up</button></span>
    </div>
</section>

<script src="/js/registration.js"></script>

</body>
</html>